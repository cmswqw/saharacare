import { TriangleAlert } from "lucide-react";
export function AlertCard({ title, detail, tone = "warning" }: { title: string; detail: string; tone?: "warning" | "danger" }) { return <div className={`flex gap-3 rounded-2xl border p-4 ${tone === "danger" ? "border-red-200 bg-red-50 text-red-950 dark:bg-red-950/30 dark:text-red-100" : "border-amber-200 bg-amber-50 text-amber-950 dark:bg-amber-950/30 dark:text-amber-100"}`}><TriangleAlert className="h-6 w-6 shrink-0" /><div><p className="font-extrabold">{title}</p><p className="mt-1 text-sm opacity-80">{detail}</p></div></div>; }
