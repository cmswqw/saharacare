import { BellRing, CheckCircle2, Clock3 } from "lucide-react";
export function ActivityFeed({ items }: { items: string[] }) { return <div className="space-y-1">{items.map((item, index) => <div key={`${item}-${index}`} className="grid grid-cols-[42px_1fr] gap-3 pb-4"><div className="relative flex justify-center">{index < items.length - 1 ? <span className="absolute bottom-0 top-9 w-px bg-slate-200 dark:bg-slate-700" /> : null}<span className="relative grid h-9 w-9 place-items-center rounded-full bg-blue-50 text-primary dark:bg-blue-950">{item.includes("taken") ? <CheckCircle2 className="h-5 w-5" /> : item.includes("Reminder") ? <BellRing className="h-5 w-5" /> : <Clock3 className="h-5 w-5" />}</span></div><p className="rounded-2xl bg-slate-50 p-3 text-sm font-semibold dark:bg-slate-800">{item}</p></div>)}</div>; }
